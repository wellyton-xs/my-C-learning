#include <stdio.h>

int main(){

	int x = 10; // assignment "="
	x += 5; // assignment "+="

	/*/

		operator:	example:
		[=]  		( x = 5 )
		[+=] 		( x += 5 )
		[-=] 		( x -= 5 )
		[*=] 		( x *= 5 )
		[/=] 		( x /= 5 )
		[%=] 		( x %= 5 )
		[&=] 		( x &= 5 )
		[|=]		( x |= 5 )
		[^=]		( x ^= 5 )
		[>>=]		( x >>= 5 )
		[<<=]		( x <<= 5 )
	/*/
	
	return 0;
}
