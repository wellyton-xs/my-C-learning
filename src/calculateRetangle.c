#include <stdio.h>

int main(){
	int length = 4;
	int width = 6;
	int area;

	area = length * width;

	printf("The length is: %d\n", length);
	printf("The width is: %d\n", width);
	printf("The area is: %d", area);
	return 0;
	
}
