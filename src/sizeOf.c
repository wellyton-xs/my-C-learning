#include <stdio.h>

int main(){
  int var;
  float var2;
  double var3;
  char var4;

  printf("size of int: %lu\n", sizeof(var));
  printf("size of float: %lu\n", sizeof(var2));
  printf("size of double: %lu\n", sizeof(var3));
  printf("size of char: %lu\n", sizeof(var4));

  return 0;
}
