#include <stdio.h>

int main(){
	// variables inits with a letter or _
	// variables cannot be a reserved keyword like <int>
	// variables are case sensitive.
	// variables cannot contain special caracters like %!#$&*@¨

	printf("variables inits with a letter or _ \n");
	printf("variables cannot be a reserved keyword like <int>\n");
	printf("variables are case sensitive.\n");
	printf("variables cannot contain special caracters\n");
	return 0;
}
